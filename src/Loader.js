import React from "react";

export default () => (
  <div>
    <div
      style={{ display: "flex", justifyContent: "center", margin: "05rem" }}
      className="lds-dual-ring"
    ></div>
  </div>
);
